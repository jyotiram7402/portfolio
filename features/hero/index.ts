export { HeroSection } from "./components/hero-section";

/**
 * The slice's only public export. `AnimatedRoles`, `TechOrbit` and `ScrollCue` are
 * internal — they exist to serve this hero, and keeping them private leaves them
 * free to be reshaped without checking for outside callers.
 */
