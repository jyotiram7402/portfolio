export { NewsletterSection } from "./components/newsletter-section";
export { SubscribeForm } from "./components/subscribe-form";
export type { SubscribeFormProps } from "./components/subscribe-form";

/**
 * `SubscribeForm` is exported because the blog's post footer renders it too — the CTA at
 * the end of an article is the highest-intent moment on the site.
 */
